package com.shiftsmart.plus.config

import com.shiftsmart.plus.BuildConfig

/**
 * Central app configuration flags.
 * IS_DEBUG_MODE is automatically true for debug builds and false for release builds.
 */
object AppConfig {
    val IS_DEBUG_MODE: Boolean = false
}
